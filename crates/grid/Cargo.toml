[package]
name = "grid"
version = "0.1.0"
edition = "2024"
description = "分布式 RPC 通信层 (对应 Go internal/grid)，Phase 2 实现"

[dependencies]
base = { workspace = true }
tonic = { workspace = true }
prost = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }